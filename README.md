# monitor
## How to run this service
1. Run  `docker-compose up` from root folder
2. Update `twitter4j.properties` file with Twitter API keys
3. Run `mvn spring-boot:run` from root folder
3. Run `loopfile.sh`